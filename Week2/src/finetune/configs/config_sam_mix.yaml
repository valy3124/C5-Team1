model:
  name: "sam_vit_b"
  model_id: "facebook/sam-vit-base"

training:
  epochs: 15
  batch_size: 4
  lr: 0.00005
  weight_decay: 0
  optimizer: "AdamW"
  scheduler:
    name: "none"
  gradient_clipping: 1.0
  aug_strategy: "no_aug"
  prompt_type: "mix"
  text_prompt: "Person. Car"

data:
  mode: "full"
  seed: 42
  split_ratio: 0.8
  root: "~/mcv/datasets/C5/KITTI-MOTS/"
  num_workers: 4
  
project: "Week2-SAM-finetuning-mix"
output_dir: "/ghome/group01/C5/benet/C5-Team1/Week2/results_finetune"
